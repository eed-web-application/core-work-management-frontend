name: Build and Publish Docker Image
on:
  push:
    branches:
      - main
jobs:
  create-image:
    uses: eed-web-application/core-work-management-deployment/.github/workflows/create-image.yml@main
    secrets: inherit
